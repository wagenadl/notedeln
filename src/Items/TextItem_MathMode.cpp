// MathMode.cpp

#include "TextItem.H"
#include <QKeyEvent>

#if 0
bool TextItem::keyPressMath(QKeyEvent *e) {
  int key = e->key();
  Qt::KeyboardModifiers mod = e->modifiers();
  QString txt = e->text();
  switch (key) {
  default:
  }
  return false;
}
#endif
 
